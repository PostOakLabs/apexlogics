{
  "tool_id": "sc67-pharma-serialization-custody",
  "al_id": "SC-67",
  "mcp_name": "pharma_serialization_custody",
  "version": "1.0.0",
  "title": "Pharma Serialization and Saleable Returns",
  "description": "Given a GTIN, serial, lot, and up to three custody steps (party, event type), returns the GTIN mod-10 check result with expected and entered check digits, step count, sequence validity against the commissioned, shipped, received, returned, decommissioned order, the first break step, a unit digest, and a status. Deterministic, zero external data. Policy Mandate v2.0: pharma_serialization_custody_record.",
  "category": "showcase",
  "cluster": "goods-provenance",
  "journeys": [],
  "personas": ["wholesale distributor"],
  "tags": ["dscsa", "gs1", "pharma", "serialization", "showcase"],
  "status": "showcase",
  "maintenance": "zero",
  "data_vintage": "GS1 General Specifications",
  "frozen_baseline": false,
  "ap2_export": true,
  "ap2_mandate_types": ["pharma_serialization_custody_record"],
  "downstream_handoff_candidates": [],
  "regulatory_citations": [
    "GS1 General Specifications",
    "US Drug Supply Chain Security Act (DSCSA)"
  ],
  "input_schema": {
    "type": "object",
    "properties": {
      "gtin": { "type": "string" },
      "serial": { "type": "string" },
      "lot": { "type": "string" },
      "steps": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "party": { "type": "string" },
            "eventType": { "type": "string", "enum": ["commissioned", "shipped", "received", "returned", "decommissioned"] }
          }
        }
      }
    },
    "required": ["gtin"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "gtin_valid": { "type": "boolean" },
      "check_digit_expected": { "type": "integer" },
      "check_digit_entered": { "type": "integer" },
      "step_count": { "type": "integer" },
      "sequence_valid": { "type": "boolean" },
      "first_break_step": { "type": "integer" },
      "unit_digest": { "type": "integer" },
      "status": { "type": "string" }
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "index.html",
    "function_name": "calculate",
    "timeout_ms": 3000
  },
  "mcp_tool_definition": {
    "name": "pharma_serialization_custody",
    "description": "Given a GTIN, serial, lot, and custody steps, returns the GTIN check result, step count, sequence validity, the first break step, a unit digest, and a status. Deterministic, zero external data.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "gtin": { "type": "string" },
        "serial": { "type": "string" },
        "lot": { "type": "string" },
        "steps": { "type": "array" }
      },
      "required": ["gtin"]
    }
  },
  "source_card": "https://ainumbers.co/chaingraph/ocg-industries.html#c12-1",
  "chain": "showcase-pharma-serialization-custody",
  "sister_suite": "https://ainumbers.co",
  "consulting_site": "https://postoaklabs.com",
  "license": "CC-BY-4.0"
}
