{
  "tool_id": "sc76-deepfake-consumer-verification",
  "al_id": "SC-76",
  "mcp_name": "deepfake_consumer_verification",
  "version": "1.0.0",
  "title": "Deepfake Consumer Verification",
  "description": "Given five yes, no, or unknown answers (credential present, credential matches source, publisher identifiable, disclosure label visible, independent report found), returns a signal total out of 10, unknown and no counts, a confidence band, a checkable-now flag, and a status. Deterministic, zero external data. Policy Mandate v2.0: deepfake_consumer_verification_record.",
  "category": "showcase",
  "cluster": "ai-software-integrity",
  "journeys": [],
  "personas": ["ordinary viewer checking a clip before resharing it"],
  "tags": ["deepfake", "c2pa", "ai-act", "consumer-verification", "showcase"],
  "status": "showcase",
  "maintenance": "zero",
  "data_vintage": "C2PA Specification 2.1",
  "frozen_baseline": false,
  "ap2_export": true,
  "ap2_mandate_types": ["deepfake_consumer_verification_record"],
  "downstream_handoff_candidates": [],
  "regulatory_citations": [
    "C2PA Specification 2.1",
    "Regulation (EU) 2024/1689 (AI Act) Art.50"
  ],
  "input_schema": {
    "type": "object",
    "properties": {
      "credential_present": { "type": "string", "enum": ["yes", "unknown", "no"] },
      "credential_matches_source": { "type": "string", "enum": ["yes", "unknown", "no"] },
      "publisher_identifiable": { "type": "string", "enum": ["yes", "unknown", "no"] },
      "disclosure_label_visible": { "type": "string", "enum": ["yes", "unknown", "no"] },
      "independent_report_found": { "type": "string", "enum": ["yes", "unknown", "no"] }
    },
    "required": ["credential_present", "credential_matches_source", "publisher_identifiable", "disclosure_label_visible", "independent_report_found"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "signal_total": { "type": "integer" },
      "unknown_count": { "type": "integer" },
      "no_count": { "type": "integer" },
      "confidence_band": { "type": "string" },
      "checkable_now": { "type": "boolean" },
      "status": { "type": "string" }
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "index.html",
    "function_name": "calculate",
    "timeout_ms": 3000
  },
  "mcp_tool_definition": {
    "name": "deepfake_consumer_verification",
    "description": "Given five yes, no, or unknown answers about a piece of content, returns a signal total, unknown and no counts, a confidence band, a checkable-now flag, and a status. Deterministic, zero external data. Scores what could be checked, not proof of authenticity or manipulation.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "credential_present": { "type": "string", "enum": ["yes", "unknown", "no"] },
        "credential_matches_source": { "type": "string", "enum": ["yes", "unknown", "no"] },
        "publisher_identifiable": { "type": "string", "enum": ["yes", "unknown", "no"] },
        "disclosure_label_visible": { "type": "string", "enum": ["yes", "unknown", "no"] },
        "independent_report_found": { "type": "string", "enum": ["yes", "unknown", "no"] }
      },
      "required": ["credential_present", "credential_matches_source", "publisher_identifiable", "disclosure_label_visible", "independent_report_found"]
    }
  },
  "source_card": "https://ainumbers.co/chaingraph/ocg-industries.html#c13-3",
  "chain": "showcase-deepfake-consumer-verification",
  "sister_suite": "https://ainumbers.co",
  "consulting_site": "https://apexadvisory.site",
  "license": "CC-BY-4.0"
}
