{
  "tool_id": "sc61-cicd-build-provenance",
  "al_id": "SC-61",
  "mcp_name": "cicd_build_provenance",
  "version": "1.0.0",
  "title": "CI/CD Build Provenance",
  "description": "Given a build descriptor entered once as the original and once as a rebuild (source ref, builder id, entrypoint, artifact name) and a claimed SLSA level, returns fingerprints for each descriptor, whether the rebuild reproduces the original, a mutated field count, field completeness, and the SLSA level the descriptor set can support. Deterministic, zero external data. Policy Mandate v2.0: cicd_build_provenance_record.",
  "category": "showcase",
  "cluster": "software-supply-chain",
  "journeys": [],
  "personas": ["platform engineer"],
  "tags": ["slsa", "build-provenance", "in-toto", "supply-chain", "showcase"],
  "status": "showcase",
  "maintenance": "zero",
  "data_vintage": "SLSA v1.0 Build Track",
  "frozen_baseline": false,
  "ap2_export": true,
  "ap2_mandate_types": ["cicd_build_provenance_record"],
  "downstream_handoff_candidates": [],
  "regulatory_citations": [
    "SLSA v1.0 Build Track",
    "in-toto attestation predicate"
  ],
  "input_schema": {
    "type": "object",
    "properties": {
      "original": {
        "type": "object",
        "properties": {
          "sourceRef": { "type": "string" },
          "builderId": { "type": "string" },
          "entrypoint": { "type": "string" },
          "artifactName": { "type": "string" }
        }
      },
      "rebuild": {
        "type": "object",
        "properties": {
          "sourceRef": { "type": "string" },
          "builderId": { "type": "string" },
          "entrypoint": { "type": "string" },
          "artifactName": { "type": "string" }
        }
      },
      "slsa_level_claimed": { "type": "integer", "enum": [1, 2, 3] }
    },
    "required": ["slsa_level_claimed"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "descriptor_a_digest": { "type": "integer" },
      "descriptor_b_digest": { "type": "integer" },
      "reproduced": { "type": "boolean" },
      "mutated_field_count": { "type": "integer" },
      "field_completeness": { "type": "integer" },
      "slsa_level_claimed": { "type": "integer" },
      "level_supportable": { "type": "integer" },
      "status": { "type": "string" }
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "index.html",
    "function_name": "calculate",
    "timeout_ms": 3000
  },
  "mcp_tool_definition": {
    "name": "cicd_build_provenance",
    "description": "Given an original and a rebuild descriptor (source ref, builder id, entrypoint, artifact name) and a claimed SLSA level, returns fingerprints, a reproduced flag, a mutated field count, field completeness, and the SLSA level the descriptor set can support. Deterministic, zero external data.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "original": { "type": "object" },
        "rebuild": { "type": "object" },
        "slsa_level_claimed": { "type": "integer", "enum": [1, 2, 3] }
      },
      "required": ["slsa_level_claimed"]
    }
  },
  "source_card": "https://ainumbers.co/chaingraph/ocg-industries.html#c10-4",
  "chain": "showcase-cicd-build-provenance",
  "sister_suite": "https://ainumbers.co",
  "consulting_site": "https://apexadvisory.site",
  "license": "CC-BY-4.0"
}
