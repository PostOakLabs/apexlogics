{
  "tool_id": "al-35-federal-vs-private-comp-comparator",
  "al_id": "AL-35",
  "version": "1.0.0",
  "title": "Federal vs. Private Sector Comp Comparator",
  "description": "Full GS-5 through GS-15 pay table (2026 OPM) with 15 locality percentages. Calculates FERS pension NPV, FEHB value, TSP match, and leave value vs. private sector offer for a 20-year lifetime value comparison. Reveals how pension NPV often reverses apparent base pay gaps.",
  "category": "compensation_offers",
  "journeys": ["compensation_benefits"],
  "personas": ["working_professional", "career_changer"],
  "tags": ["federal", "government", "gs-pay", "fers", "pension", "compensation"],
  "status": "shipped",
  "shipped_date": "2026-05-28",
  "maintenance": "low",
  "data_vintage": "OPM 2026 GS Pay Tables, FEHB 2025 enrollee share data",
  "frozen_baseline": false,
  "input_schema": {
    "type": "object",
    "properties": {
      "gs_grade": { "type": "integer", "minimum": 5, "maximum": 15 },
      "gs_step": { "type": "integer", "minimum": 1, "maximum": 10 },
      "locality": { "type": "string" },
      "coverage": { "type": "string", "enum": ["self_only", "self_and_family"] },
      "private_base": { "type": "number" },
      "private_bonus": { "type": "number" },
      "private_match_pct": { "type": "number" },
      "years_projected": { "type": "integer" }
    },
    "required": ["gs_grade", "gs_step", "locality", "coverage"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "federal_total_annual": { "type": "number" },
      "private_total_annual": { "type": "number" },
      "federal_20yr_value": { "type": "number" },
      "private_20yr_value": { "type": "number" },
      "pension_npv": { "type": "number" },
      "winner": { "type": "string" }
    }
  },
  "mcp_tool_definition": {
    "name": "compare_federal_private_comp",
    "description": "Given GS grade/step/locality and a private-sector offer, returns annual and 20-year lifetime value comparison including FERS pension NPV, FEHB value, and TSP match. Uses embedded 2026 OPM pay tables. Deterministic.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "gs_grade": { "type": "integer" },
        "gs_step": { "type": "integer" },
        "locality": { "type": "string" },
        "private_base": { "type": "number" }
      },
      "required": ["gs_grade", "gs_step", "locality"]
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "index.html",
    "function_name": "compareComp",
    "timeout_ms": 3000
  },
  "ap2_export": true,
  "ap2_mandate_types": ["federal_comp_record"],
  "downstream_handoff_candidates": ["AL-01", "AL-04"],
  "sister_suite": "https://ainumbers.co",
  "consulting_site": "https://apexadvisory.site",
  "license": "CC-BY-4.0",
  "regulatory_citations": [
    "OPM General Schedule Pay Tables 2026",
    "OPM FEHB 2025 Premiums — Biweekly Employee Contributions",
    "FERS Handbook — Chapter 50, Retirement Benefits"
  ]
}
